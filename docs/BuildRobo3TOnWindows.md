# Build instructions

The legacy MongoDB server/Qt 5 build has been removed.
See [the current build instructions](../README.md#build-on-macos).

The verified build target is native macOS ARM64. Other platforms require
matching native dependencies supplied to the CMake cache variables.
