#!/usr/bin/env bash
set -euo pipefail
repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec cmake -S "$repo_dir" -B "$repo_dir/build/arm64" -DCMAKE_OSX_ARCHITECTURES=arm64 "-DCMAKE_INSTALL_PREFIX=$repo_dir/build/arm64/install" "$@"
