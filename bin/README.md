# Build shortcuts

Use `bash scripts/build-macos.sh` for dependency bootstrap, build, tests and
packaging. The Unix shortcuts in this folder operate on `build/arm64`.
Historical Windows/code-analysis helpers are not part of the verified ARM build.
