#!/usr/bin/env bash
set -euo pipefail
repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cmake --build "$repo_dir/build/arm64" --parallel "${ROBOMONGO_JOBS:-6}"
exec ctest --test-dir "$repo_dir/build/arm64" --output-on-failure "$@"
